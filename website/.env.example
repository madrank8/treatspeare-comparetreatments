# Optional — site builds and runs locally with all of these unset.

# Cookiebot CMP Domain Group ID
NEXT_PUBLIC_COOKIEBOT_ID=

# Google Tag Manager container (GTM-XXXXXX)
NEXT_PUBLIC_GTM_ID=

# Upstash / Vercel KV — affiliate click logging in /go/[slug]
KV_REST_API_URL=
KV_REST_API_TOKEN=
